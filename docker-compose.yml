services:
  # ui:
  #   build:
  #     context: ui
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8501:8501"

  api:
    build:
      context: api
      dockerfile: Dockerfile
    ports:
      - "8002:8002"
    env_file:
      - .env


  gradio:
    build:
      context: ui
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    depends_on:
      - api

