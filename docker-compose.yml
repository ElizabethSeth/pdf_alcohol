services:
  # ui:
  #   build:
  #     context: ui
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8501:8501"

  api:
    container_name: api_service
    build:
      context: api
      dockerfile: Dockerfile
    ports:
      - "8003:8003"
    env_file:
      - .env


  gradio:
    container_name: gradio_ui
    build:
      context: ui
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    depends_on:
      - api

